```python
# Define content for README.md matching the developed AX management system
readme_content = """# 🚀 AX(AI 부서 혁신) 과제 통합 관리 시스템

본 시스템은 조직 내부의 **AX(AI Transformation, 인공지능 전환)** 및 부서 혁신 과제를 효율적으로 등록, 관리, 시각화하기 위해 개발된 Streamlit 기반의 사내 플랫폼입니다. 

기존의 단일 부서 분류 체계에서 벗어나 **부서(국 = Bureau)**와 **부서(과 = Team)**의 계층적 구조를 완벽히 지원하며, 조직도 설정 파일(`organization.json`)과 동적으로 연동되어 데이터 입력의 무결성을 보장합니다.

---

## 🌟 주요 기능

### 1. 전사 AX 종합 대시보드 (`📊 Dashboard`)
* **핵심 지표(KPI) 제공:** 총 추진 과제 수, 참여 국(부서) 수, 전사 예산 및 사업비 합계 자동 집계 및 표시.
* **적용 완료율 게이지 차트:** 전체 과제 중 '적용 완료' 단계에 도달한 과제의 비율을 실시간 시각화.
* **카테고리별 비중 도넛 차트:** 제도, 사업, 예산안, 심사 등 카테고리별 과제 분포 확인.
* **국/과 계층별 트리맵 (Treemap):** 카테고리 내부에서 어떤 국과 과가 과제를 주도하고 있는지 계층 구조로 시각화.
* **진행 단계 및 부서별 스택 바 차트:** 국별 과제 진행 상태(아이디어, 기획 중, 개발 중, 적용 완료)를 한눈에 비교.

### 2. 전사 과제 조회 (`👁️ Read-only View`)
* 전사에서 추진 중인 모든 AX 과제를 통합 테이블 형태로 조회.
* **다중 필터링 지원:** 카테고리, 부서(국), 부서(과) 단위로 필터를 조합하여 원하는 과제 데이터만 신속하게 필터링 및 확인 가능.

### 3. 우리 부서 과제 관리 및 등록 (`📝 Department Management`)
* **소속 부서 자동 바인딩:** 로그인한 사용자의 국/과 세션 정보를 기반으로 해당 부서의 과제만 안전하게 필터링 및 관리.
* **동적 데이터 에디터 (`st.data_editor`):** 스프레드시트 형태로 기존 과제 정보를 실시간으로 수정하고, 예산/사업비 등 숫자 필드는 정수형 타입 검증 및 포맷팅 지원.
* **EAV(Entity-Attribute-Value) 유연성:** 시스템 어드민이 설정한 카테고리별 메타데이터 항목(진행상태, 담당자, 관련법령, 활용시스템 등)이 동적으로 양식에 반영되어 신규 과제 등록 가능.

### 4. 조직도 연동 및 Dropdown 무결성 (`🏢 Organization Cascading`)
* `organization.json`에 정의된 데이터 구조를 기반으로 **국 선택 시 소속 과 목록이 동적으로 변경**되는 상호 연동형 드롭다운(Cascading Selectbox) 시스템 적용.
* 직접 타이핑 시 발생할 수 있는 오탈자 및 부서명 불일치 문제를 원천 차단.
* 관리자 계정의 소속 전환 및 신규 사용자 계정 생성 시 조직도 드롭다운이 완벽하게 적용되며, 조직도 파일 부재 시 텍스트 입력으로 전환되는 Fallback 안전장치 내장.

### 5. 시스템 관리자 메뉴 (`⚙️ Admin Options`)
* **카테고리 동적 확장:** 신규 과제 카테고리를 실시간으로 추가하고, 해당 카테고리에서 관리할 항목(컬럼 속성)을 쉼표(,) 구분으로 유연하게 정의 가능.
* **기존 카테고리 항목 추가:** 운영 중인 카테고리에 새로운 메타데이터 속성을 즉시 주입 가능.
* **사용자 생성 기능:** 아이디, 비밀번호, 권한(user/admin) 및 조직도 드롭다운 기반의 국/과 소속을 지정하여 현장에서 신규 계정 즉시 발급.

---

## 🛠️ 시스템 아키텍처 및 기술 스택

* **Frontend/Application:** Streamlit (Python 기반 로우코드 웹프레임워크)
* **Visualization:** Plotly Express & Plotly Graph Objects (인터랙티브 차트 구현)
* **Database:** SQLite3 (EAV 데이터 모델 설계를 통한 데이터 무결성 및 확장성 확보)
  * 동시성 및 성능 최적화를 위해 `WAL(Write-Ahead Logging)` 모드 및 `NORMAL` 동기화 적용.
* **Authentication:** SHA-256 단방향 암호화 해시 기반 세션 로그인 시스템.
* **Configuration:** JSON 기반 메타데이터 모델(`model_config.json`) 및 계층형 조직도 설정(`organization.json`).

---

## 📂 주요 파일 구조


```

```text
README.md generated successfully.

```text
├── app.py                  # Streamlit 메인 애플리케이션 소스 코드
├── ax_management.db        # 과제 및 사용자 정보가 저장되는 SQLite 데이터베이스
├── model_config.json       # 카테고리별 관리 항목(메타데이터 속성) 설정 파일
└── organization.json       # 드롭다운 연동을 위한 전사 계층형 조직도 파일

```

---

## 🚀 시작하기 (Installation & Running)

### 1. 필수 라이브러리 설치

본 시스템을 실행하기 위해 필요한 Python 패키지를 설치합니다.

```bash
pip install streamlit pandas plotly

```

### 2. 환경 설정 파일 준비

프로젝트 루트 디렉토리에 조직 구조에 맞는 `organization.json` 파일을 아래 형식을 참고하여 생성합니다.

```json
{
    "디지털융합심사국": [
        "인공지능빅데이터심사과",
        "융합기술심사과"
    ],
    "지식재산정보국": [
        "지식재산정책과",
        "정보기획과"
    ],
    "지식재산보호협력국": [
        "지식재산보호정책과"
    ]
}

```

### 3. 애플리케이션 실행

설치가 완료되면 다음 명령어를 사용하여 Streamlit 서버를 가동합니다.

```bash
streamlit run app.py

```

### 4. 테스트 계정 정보

최초 DB 빌드 시 자동으로 생성되는 시스템 테스트용 계정 리스트입니다.

* **시스템 관리자 (Admin):** `admin` / `admin123`
* **일반 사용자 (User):**
* `kim` / `kim123` (소속: 디지털융합심사국 / 인공지능빅데이터심사과)
* `lee` / `lee123` (소속: 지식재산정보국 / 지식재산정책과)
* `park` / `park123` (소속: 지식재산보호협력국 / 지식재산보호정책과)



---

## 🔒 데이터 보안 및 확장 안내

* **EAV 스키마:** 본 시스템은 속성 확장이 자유로운 **EAV(Entity-Attribute-Value)** 설계가 적용되어 있어, `model_config.json`이나 관리자 메뉴를 통해 항목을 변경하더라도 별도의 데이터베이스 마이그레이션(DDL 수정) 없이 안정적으로 구동됩니다.
* **세션 관리:** 각 사용자의 권한(`admin` / `user`) 및 국/과 소속은 로그인 시 Streamlit Session State에 캡슐화되어 관리되므로, 타 부서의 과제 데이터를 임의로 수정하거나 변조하는 접근을 차단합니다.
"""
